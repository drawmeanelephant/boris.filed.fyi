# OS
.DS_Store

# Tooling / sessions (never commit)
.boris-sessions/
.freebuff/

# The agent kit is a local transport artifact, not the site source
boris-agent-kit/

# Preview scratch
.preview/

# Stray llms.txt at docs root (the real one is dist/llms.txt)
docs/llms.txt

# Boris build cache (regenerated on build)
docs/dist/.boris-cache/

# Wrangler local cache
.wrangler/
docs/.wrangler/
